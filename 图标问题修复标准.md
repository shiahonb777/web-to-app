1. Adaptive Icon XML (mipmap-anydpi-v26/ic_launcher.xml)
   └── 保留原样，通过 else 分支正常复制

2. ARSC 路径替换 (forceReplaceIconPaths)
   ├── res/drawable/ic_launcher_foreground.xml → .png ✅
   ├── res/drawable-v24/ic_launcher_foreground.xml → .png ✅
   └── res/drawable-anydpi-v24/ic_launcher_foreground.xml → .png ✅

3. 写入 foreground PNG (addAdaptiveIconPngs)
   ├── res/drawable/ic_launcher_foreground.png
   ├── res/drawable-v24/ic_launcher_foreground.png
   └── res/drawable-anydpi-v24/ic_launcher_foreground.png

4. 系统加载 Adaptive Icon
   ├── 解析 ic_launcher.xml → 找到 foreground 和 background 引用
   ├── foreground → 加载我们的 PNG（用户图标 + safe zone）
   └── background → 保持原样（模板的默认背景）